using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WaspAnimations : MonoBehaviour
{

    [SerializeField]
    private Animator waspAnim;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    public void MovementAnims()
    {
       // doorAnim.Play("Door Open");
        // doorAnimationControl.SetTrigger("OpenDoor");
    }
    public void ShootAnims()
    {
       // doorAnim.Play("Door Close");
        // doorAnimationControl.SetTrigger("CloseDoor");
    }
    // Update is called once per frame
    void Update()
    {
        
    }
}
